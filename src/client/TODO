  * Command-line history: arrow up == revive earlier post for edit
  * When notification is unsupported, suggest to either use Chrome or
    try the Firefox plugin "html-notifications"
  * Display sensible info when JavaScript is disabled
  * Grey out or cross over notification button when unsupported
  * alt text for logo, and javascript popup asking if ok to leave chat
  * alt text for "?" describing that that it generates random room name
  * conditionalize login popup based on whether Chrome already installed
  * extend login popup to also mention Chromium (not only Chrome)
  * Fix support escaping from random number dialog (appears as frozen)
  * Use http://slexaxton.github.com/Jed/ for l10n (not hardcoded strings)
